import {LocationType} from '@gamepark/expedition/material/ExpeditionLocations'
import {PlaceLocation} from './PlaceLocation'

export const LocationsDescription = {
  [LocationType.Place]: PlaceLocation
}